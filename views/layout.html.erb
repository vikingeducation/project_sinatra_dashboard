<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>

		<link rel="stylesheet" href="<%= request.base_url%>/assets/styles/css/bootstrap.min.css"/>

		<title>Job Hunt Central</title>
	</head>
	<body>
		<main class="container">
			<h1>Job Hunt Central</h1>

			<% if params[:notice] %>
				<div class="alert alert-success" role="alert">
					<%= params[:notice] %>
				</div>
			<% end %>

			<% if params[:error] %>
				<div class="alert alert-danger" role="alert">
						<%= params[:error] %>
				</div>
			<% end %>

			<%= yield %>
		</main>
		<script src="<%= request.base_url %>/assets/js/jquery.min.js"></script>
		<script src="<%= request.base_url %>/assets/js/bootstrap.min.js"></script>
	</body>
</html>